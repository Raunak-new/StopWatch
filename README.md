# StopWatch
A simple and responsive stopwatch built with React using useState,useRef,useEffect. Features start, stop, and reset controls, smooth animations, and a modern glass-effect UI. Time updates every 10ms and displays hours, minutes, seconds, and milliseconds
